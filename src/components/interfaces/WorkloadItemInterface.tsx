export interface WorkloadItemInterface {
  id: string;
  complexity: number;
  status: string;
  name: string;
  isCompleted: boolean;
}
//This interface would be used to standardise the properties of each workload.